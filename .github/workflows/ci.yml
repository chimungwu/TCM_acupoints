- name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          # 暫時移除 cache 參數以排除快取路徑問題

      - name: Install dependencies
        # 確保在根目錄執行 npm install (因為 package-lock.json 在根目錄)
        run: npm install
